# Assets Source and Attribution

The mesh and texture files in this folder were imported to make `aloha.xml` load and run locally.

Primary source:

- `google-deepmind/mujoco_menagerie` (ALOHA model assets)
- Repository: https://github.com/google-deepmind/mujoco_menagerie
- Path used: `aloha/assets/`

License:

- See upstream project license and model-specific notices in `mujoco_menagerie/aloha`.

Notes:

- Filenames are intentionally kept consistent with `aloha.xml` mesh references.
- If you replace a file, keep naming consistent or update XML references accordingly.

